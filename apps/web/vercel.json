{
  "name": "tradygo-web",
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile --prod=false && pnpm --filter @tradygo/ui build && pnpm run build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install --frozen-lockfile --prod=false",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_API_URL": "https://api.tradygo.in"
  },
  "functions": {
    "app/**/*.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://api.tradygo.in/api/:path*"
    }
  ]
}