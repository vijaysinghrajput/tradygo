{
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @tradygo/ui build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "functions": {
    "apps/web/.next/**/*.js": {
      "maxDuration": 30
    },
    "apps/admin/.next/**/*.js": {
      "maxDuration": 30
    }
  }
}
